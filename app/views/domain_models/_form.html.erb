<%= form_with(model: domain_model, local: true) do |form| %>
  <% if domain_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(domain_model.errors.count, "error") %> prohibited this domain_model from being saved:</h2>

      <ul>
      <% domain_model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :domain_model_name %>
  </div>

  <div class="field">
    <%= form.label :desc %>
    <%= form.text_area :desc, id: :domain_model_desc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
